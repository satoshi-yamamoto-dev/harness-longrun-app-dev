# 0.1.0 公開前レビュー

2026-09-06。ローカル配布候補の作成と技術確認まで実施。GitHubへの公開、tag、Release、Pages設定は未実施。

## 配布候補

開発作業コピーの `.longrun-app-dev/releases/0.1.0-candidate-20260906/` に83ファイルを配置した。`release-manifest.json` に各ファイルのSHA-256とサイズ、`runtime/dist/build-manifest.json` にビルド成果物と依存一覧を保存した。候補は上書きしない。再作成時は新しい保存先を指定する。

現在のビルドが生成したファイルだけを採用し、元ファイルとコピー後の両方でハッシュを確認する。改変済み成果物、資格情報用ファイル、プロジェクト外へのパス、シンボリックリンク、既存の配布先を拒否する。既知の秘密キー形式を内容検査するが、任意の個人情報・未知の秘密値の完全検出ではない。

## 検証

- 配布・MCP解決・改変検知・資格情報ファイル拒否などの5試験が成功。
- 配布候補からversion、init、status、doctorを実行でき、既存設定が保持されることを確認。
- Pluginの名前・版・リポジトリ・ライセンスの整合性検証が成功。
- 古いビルド成果物の混入を防止。ビルド成功後、生成物ディレクトリ内の不要なJS・source mapだけを整理する。
- npm／ブラウザ起動・実モデル統合は[暫定検証記録](provisional-validation.md)の制約を引き継ぐ。

## ライセンス

このプロジェクトとPlugin本体はMITで、両方のLICENSE本文は一致する。バンドルされた依存のライセンスをMITへ置き換えない。ビルド時に元のライセンスコメントを保持し、依存パッケージのライセンス本文を `runtime/dist/THIRD_PARTY_NOTICES.txt` へ収録する。

Agent SDKは独自の利用条件に従う。利用者自身の認証と契約が必要であり、認証情報は配布候補に含めない。[Anthropicの公式案内](https://code.claude.com/docs/en/legal-and-compliance)を参照する。このレビューは法的適合性の承認ではない。

## 残る公開条件

1. 画像の機密情報検出と任意コードの外部通信隔離は未実装。現版の利用範囲は専用のローカル検証環境とする。
2. 長時間実モデルの品質合否は未判定。暫定完了の試験を品質保証として告知しない。
3. 公開用リポジトリ、remote、Secrets、Pages、branch protectionは未設定・未確認。作業ファイルは未コミット。
4. 最終公開の承認は未取得。現在の候補を公開済みとして案内しない。

## リリースノート草案

0.1.0は開発評価用の初期版。仕様作成、Build/QA反復、予算・時間停止、Gitチェックポイント、停止・再開・ロック回収、状態・環境確認、テキストのシークレットマスク、直接ツール操作の拒否を備える。MCP依存先を別フォルダで指定できる。実利用での品質評価と環境互換性の検証は継続中。
