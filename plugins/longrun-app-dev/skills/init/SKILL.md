---
name: init
description: Initialize longrun-app-dev configuration in the current project. Use when the user invokes /longrun-app-dev:init or asks to initialize this harness.
---

# Initialize longrun-app-dev

Run `longrun-app-dev init $ARGUMENTS` with the Bash tool from the current project directory.

Report the command result to the user. Do not claim that initialization completed unless the CLI reports success.
